<div class="row">
  <div class="small-12 columns">
    <h1>Editar Examen Diagnostico</h1>

    <%= simple_form_for [:admin, @diagnostic_exam] do |f| %>
      <%= display_flash_messages %>

      <%= f.input :subject %><br>
      <%= f.button :submit %>

    <% end %>
  </div>
</div>

<div class="row">
  <div class="small-12 columns">
    <h4>Preguntas Actuales</h4>
  </div>
</div>

<div class="row">
  <% @questions.each do |question| %>
    <div class="small-12 columns">
      <%= question.question %> | <%= link_to 'Editar', edit_admin_diagnostic_exam_question_path(@diagnostic_exam, question) %>
    </div>
  <% end %>
</div>

<div class="row">
  <div class="small-12 columns">
    <%= link_to 'Agregar Preguntas', new_admin_diagnostic_exam_question_path(@diagnostic_exam), class: 'button tiny radius success' %>
  </div>
</div>

<div class="row">
  <div class="small-12 columns">
    <%= link_to 'Regresar', admin_dashboard_path(anchor: 'examenes_diagnositcos'), class: 'button tiny radius' %>
  </div>
</div>
