<!DOCTYPE html>
<html lang="en">
  <head>
    <title>DynamicTeacher</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <div style="right: 10%; top: 10px; position: relative; ">
        <%= link_to 'Cerrar Sesión', destroy_user_session_path, class: 'right small button radius alert', method: :delete  %>
      </div>
    <% end %>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
