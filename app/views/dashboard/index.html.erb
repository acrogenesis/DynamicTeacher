<div class="row">
  <div class="small-6 columns">
    <h2><%= fa_icon 'tachometer' %> Dashboard</h2>
  </div>
  <div class="small-6 columns">
    <%= link_to 'Cerrar Sesión', destroy_user_session_path, class: 'right small button radius', method: :delete  %>
  </div>
</div>
<%= display_flash_messages %>
<div class="row">
  <div class="small-12 columns">
    <dl class="tabs" data-tab data-options="deep_linking:true">
      <dd class="active"><a href="#tareas-pendientes"><%= fa_icon 'files-o' %> Tareas Pendientes</a></dd>
      <dd><a href="#calificaciones"><%= fa_icon 'files-o' %> Calificaciones</a></dd>
      <dd><a href="#ejercicios"><%= fa_icon 'file-code-o' %> Ejercicios</a></dd>
      <dd><a href="#videos"><%= fa_icon 'video-camera' %> Videos</a></dd>
    </dl>
    <div class="tabs-content">
      <div class="content active" id="tareas-pendientes">
        <%= render 'tareas' %>
      </div>
      <div class="content" id="calificaciones">
        <%= render 'calificaciones' %>
      </div>
      <div class="content" id="ejercicios">
        <%= render 'ejercicios' %>
      </div>
      <div class="content" id="videos">
        <%= render 'videos' %>
      </div>
    </div>
  </div>
</div>
